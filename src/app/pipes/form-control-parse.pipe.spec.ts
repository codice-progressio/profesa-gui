import { FormControlParsePipe } from './form-control-parse.pipe';

describe('FormControlParsePipe', () => {
  it('create an instance', () => {
    const pipe = new FormControlParsePipe();
    expect(pipe).toBeTruthy();
  });
});
