import { ContieneElPermisoPipe } from './contiene-el-permiso.pipe';

describe('ContieneElPermisoPipe', () => {
  it('create an instance', () => {
    const pipe = new ContieneElPermisoPipe();
    expect(pipe).toBeTruthy();
  });
});
