import { OrdenadorDeColumnasDirective } from './ordenador-de-columnas.directive';

describe('OrdenadorDeColumnasDirective', () => {
  it('should create an instance', () => {
    const directive = new OrdenadorDeColumnasDirective();
    expect(directive).toBeTruthy();
  });
});
