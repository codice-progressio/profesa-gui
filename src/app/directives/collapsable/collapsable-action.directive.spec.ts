import { CollapsableActionDirective } from './collapsable-action.directive';

describe('CollapsableActionDirective', () => {
  it('should create an instance', () => {
    const directive = new CollapsableActionDirective();
    expect(directive).toBeTruthy();
  });
});
